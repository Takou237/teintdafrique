# Configuration du build de PRODUCTION (lu par `vite build`).
# Le front Vercel appelle l'API PHP hébergée sur LWS (dossier api/ à la racine
# web de teintdafrique.com). Valeur publique — pas un secret.
VITE_API_URL=https://teintdafrique.com